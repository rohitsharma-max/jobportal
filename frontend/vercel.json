{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "npm run build",
  "outputDirectory": "dist",
  "framework": "vite",
  "rewrites": [
    {
      "comment": "Client-side routing: React Router owns every path, so serve index.html for anything that isn't a real file. Without this, refreshing on /admin/applications or opening a shared link returns a 404 from Vercel's static host.",
      "source": "/((?!assets/).*)",
      "destination": "/index.html"
    }
  ]
}
